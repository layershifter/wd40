import { makeStyles } from "@griffel/core";
import { bar } from "./tokens";

export function Component() {}

export const b = makeStyles({
  root: { color: bar },
});

export default function App() {}
